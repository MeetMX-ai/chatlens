# Development / CI dependencies
# 不引入新业务依赖到主 requirements.txt — 仅开发/CI 使用
#
# 用法：
#   pip install -r requirements.txt
#   pip install -r requirements-dev.txt

pytest>=7.0
pytest-asyncio>=0.24
pytest-cov>=4.0
pytest-xdist>=3.0
pytest-timeout>=2.0
